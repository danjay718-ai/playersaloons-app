<div class="space-y-5">
    @if(session('success'))<div class="rounded-lg border border-emerald-500/30 bg-emerald-500/10 p-3 text-sm text-emerald-200">{{ session('success') }}</div>@endif
    <div class="flex items-center justify-between"><p class="text-sm text-slate-500">Approve only genuine, appropriate player feedback for public display.</p><select wire:model.live="status" class="rounded-lg border border-slate-800 bg-slate-900 px-3 py-2 text-sm text-white"><option value="">All statuses</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option></select></div>
    <div class="grid gap-4 lg:grid-cols-2">@forelse($reviews as $review)<article class="rounded-xl border border-slate-800 bg-slate-950/60 p-5"><div class="flex justify-between"><div><p class="font-bold text-white">{{ $review->user->username }}</p><p class="text-amber-300">{{ str_repeat('★', $review->rating) }}<span class="text-slate-700">{{ str_repeat('★', 5-$review->rating) }}</span></p></div><span class="text-[10px] font-bold uppercase text-slate-500">{{ $review->status }}</span></div><p class="mt-4 text-sm leading-6 text-slate-300">{{ $review->review }}</p><textarea wire:model="notes" rows="2" placeholder="Moderation note (optional)" class="mt-4 w-full rounded-lg border border-slate-800 bg-slate-900 p-3 text-xs text-white"></textarea><div class="mt-3 flex gap-2"><button wire:click="moderate({{ $review->id }}, 'approved')" class="rounded-lg bg-emerald-600/20 px-3 py-2 text-xs font-bold text-emerald-300">Approve</button><button wire:click="moderate({{ $review->id }}, 'rejected')" class="rounded-lg bg-red-600/20 px-3 py-2 text-xs font-bold text-red-300">Reject</button></div></article>@empty<p class="text-sm text-slate-500">No reviews in this queue.</p>@endforelse</div>
    {{ $reviews->links() }}
</div>
